{
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build", "config": { "distDir": "dist" } }
  ],
  "routes": [
    { "src": "/(.*)\\.(js|css|map|json|png|jpg|jpeg|gif|svg|ico|webp|woff2?)$", "dest": "/$1.$2" },

    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
